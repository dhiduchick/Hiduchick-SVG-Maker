// Inquirer (node package manager) import
const inquirer = require('inquirer');

//file system module import
const fs = require('fs');

// importing classes fomr ./lib/shapes directory
const {Triangle, Square, Circle } = require('./lib/shapes')

